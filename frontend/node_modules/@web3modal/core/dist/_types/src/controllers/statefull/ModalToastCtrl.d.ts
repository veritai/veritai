import type { ModalToastCtrlState } from '../../../types/statefullCtrlTypes';
export declare const ModalToastCtrl: {
    state: ModalToastCtrlState;
    subscribe(callback: (newState: ModalToastCtrlState) => void): () => void;
    openToast(message: ModalToastCtrlState['message'], variant: ModalToastCtrlState['variant']): void;
    closeToast(): void;
};
